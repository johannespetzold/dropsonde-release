set -e -x

export GOROOT=$(readlink -nf /var/vcap/packages/golang)
export PATH=/var/vcap/packages/git/bin:$PATH
export PATH=$GOROOT/bin:$PATH

export GOPATH=$PWD

go install hurricanehunter
cp -a $PWD/bin/hurricanehunter ${BOSH_INSTALL_TARGET}
